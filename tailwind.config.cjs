const config = {
	mode: 'jit',
	purge: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		fontFamily: {
			display: ['Raleway'],
			body: ['Raleway']
		},
		extend: {}
	},

	plugins: []
};

module.exports = config;
